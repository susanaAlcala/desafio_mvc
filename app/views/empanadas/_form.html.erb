<%= form_with(model: empanada, local: true) do |form| %>
  <% if empanada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(empanada.errors.count, "error") %> prohibited this empanada from being saved:</h2>

      <ul>
      <% empanada.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ingrediente_ppal %>
    <%= form.text_field :ingrediente_ppal %>
  </div>

  <div class="field">
    <%= form.label :ingrediente_secundario %>
    <%= form.text_field :ingrediente_secundario %>
  </div>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
